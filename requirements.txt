# =============================================================================
# MedImage Labeler — 依赖清单
# =============================================================================
# 安装方式：在项目目录下运行 pip install -r requirements.txt
# =============================================================================

# --- Web 应用框架 -----------------------------------------------------------
streamlit>=1.28.0

# --- 图像处理 ---------------------------------------------------------------
Pillow>=10.0.0              # PIL 分支，用于图像打开/缩放/信息提取

# --- 数据处理 ---------------------------------------------------------------
pandas>=2.0.0               # 数据框操作，用于导出 CSV 和图表数据

# --- 可视化图表（Plotly 提供交互式图表）-------------------------------------
plotly>=5.17.0

# --- 图像标注辅助（可选，用于画 bounding box）-------------------------------
# 如果安装失败，标注页面会退化为手动输入坐标模式，不影响核心功能
streamlit-drawable-canvas>=0.9.0
